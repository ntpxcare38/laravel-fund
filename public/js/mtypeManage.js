setBillModal = (
    type_mid,
    type_mname,
    ) => {

    // document.getElementById('position_fid').value=(position_fid);
    // document.getElementById('position_fname').value=(position_fname);
    $('#type_mid').val(type_mid)
    $('#type_mname').val(type_mname);


}
